# MySQL source database
DB_HOST=127.0.0.1
DB_PORT=3310
DB_USER=ucacue_user
DB_PASS=change_this_before_running
DB_NAME=ucacue_erp
DB_POOL_LIMIT=5

# Google service account JSON path (explicit; no default file fallback)
GOOGLE_APPLICATION_CREDENTIALS=./path/to/service-account.json

SYNC_POLL_INTERVAL_MS=2000
