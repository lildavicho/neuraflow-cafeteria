# SRI Operational Assets

- `certs/`: monta aqui el certificado PKCS12 del emisor para el contenedor backend.
- Usa rutas del contenedor como `/app/certs/emisor.p12` en `backend/.env`.
- No versionar certificados reales ni passwords.
